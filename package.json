{
  "name": "user-management-api-monorepo",
  "version": "1.0.0",
  "description": "Complete user management system with JWT authentication, password recovery, and profile photo uploads.",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm run install:all",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n \"BACK,FRONT\" -c \"blue,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
